export type ProposalRejectedEvent = ({
    eventType: string;
    eventDate: string;
} & {
    readonly proposalId: string;
    /**
     * Generic Event reason information structure.
     *
     */
    reason?: Record<string, any>;
});
