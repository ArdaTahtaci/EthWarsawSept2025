export type PropertyQueryEvent = ({
    eventType: string;
    eventDate: string;
} & {
    propertyQuery: {
        issuerProperties?: Record<string, any>;
        queryId?: string;
        queriedProperties: Array<string>;
    };
});
