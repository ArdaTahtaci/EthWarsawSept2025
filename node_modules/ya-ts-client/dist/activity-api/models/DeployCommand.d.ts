export type DeployCommand = (Record<string, any> & {
    deploy: {
        net?: Array<{
            id: string;
            ip: string;
            mask: string;
        }>;
        hosts?: Record<string, string>;
        nodes?: Record<string, string>;
    };
});
