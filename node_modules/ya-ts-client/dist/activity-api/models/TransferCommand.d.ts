export type TransferCommand = (Record<string, any> & {
    transfer: {
        from: string;
        to: string;
        format?: string;
        depth?: number;
        fileset?: Array<any>;
    };
});
