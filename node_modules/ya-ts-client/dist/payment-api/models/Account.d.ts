/**
 * Payment account (wallet)
 */
export type Account = {
    platform: string;
    address: string;
    driver: string;
    network: string;
    token: string;
    send: boolean;
    receive: boolean;
};
